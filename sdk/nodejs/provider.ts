// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The provider type for the contabo package. By default, resources use package-wide configuration
 * settings, however an explicit `Provider` instance may be created and passed during resource
 * construction to achieve fine-grained programmatic control over provider settings. See the
 * [documentation](https://www.pulumi.com/docs/reference/programming-model/#providers) for more information.
 */
export class Provider extends pulumi.ProviderResource {
    /** @internal */
    public static readonly __pulumiType = 'contabo';

    /**
     * Returns true if the given object is an instance of Provider.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Provider {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === "pulumi:providers:" + Provider.__pulumiType;
    }

    /**
     * The api endpoint is https://api.contabo.com.
     */
    declare public readonly api: pulumi.Output<string | undefined>;
    /**
     * Your oauth2 client id can be found in the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.
     */
    declare public readonly oauth2ClientId: pulumi.Output<string | undefined>;
    /**
     * Your oauth2 client secret can be found in the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.
     */
    declare public readonly oauth2ClientSecret: pulumi.Output<string | undefined>;
    /**
     * API Password (this is a new password which you'll set or change in the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.)
     */
    declare public readonly oauth2Pass: pulumi.Output<string | undefined>;
    /**
     * The oauth2 token url is https://auth.contabo.com/auth/realms/contabo/protocol/openid-connect/token.
     */
    declare public readonly oauth2TokenUrl: pulumi.Output<string | undefined>;
    /**
     * API User (your email address to login to the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.
     */
    declare public readonly oauth2User: pulumi.Output<string | undefined>;

    /**
     * Create a Provider resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ProviderArgs, opts?: pulumi.ResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        {
            resourceInputs["api"] = (args?.api) ?? utilities.getEnv("CNTB_API", "CONTABO_API");
            resourceInputs["oauth2ClientId"] = (args?.oauth2ClientId) ?? utilities.getEnv("CNTB_OAUTH2_CLIENT_ID", "CONTABO_OAUTH2_CLIENT_ID");
            resourceInputs["oauth2ClientSecret"] = (args?.oauth2ClientSecret ? pulumi.secret(args.oauth2ClientSecret) : undefined) ?? utilities.getEnv("CNTB_OAUTH2_CLIENT_SECRET", "CONTABO_OAUTH2_CLIENT_SECRET");
            resourceInputs["oauth2Pass"] = (args?.oauth2Pass ? pulumi.secret(args.oauth2Pass) : undefined) ?? utilities.getEnv("CNTB_OAUTH2_PASS", "CONTABO_OAUTH2_PASS");
            resourceInputs["oauth2TokenUrl"] = (args?.oauth2TokenUrl) ?? utilities.getEnv("CNTB_OAUTH2_TOKEN_URL", "CONTABO_OAUTH2_TOKEN_URL");
            resourceInputs["oauth2User"] = (args?.oauth2User) ?? utilities.getEnv("CNTB_OAUTH2_USER", "CONTABO_OAUTH2_USER");
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["oauth2ClientSecret", "oauth2Pass"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(Provider.__pulumiType, name, resourceInputs, opts);
    }

    /**
     * This function returns a Terraform config object with terraform-namecased keys,to be used with the Terraform Module Provider.
     */
    terraformConfig(): pulumi.Output<Provider.TerraformConfigResult> {
        return pulumi.runtime.call("pulumi:providers:contabo/terraformConfig", {
            "__self__": this,
        }, this);
    }
}

/**
 * The set of arguments for constructing a Provider resource.
 */
export interface ProviderArgs {
    /**
     * The api endpoint is https://api.contabo.com.
     */
    api?: pulumi.Input<string | undefined>;
    /**
     * Your oauth2 client id can be found in the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.
     */
    oauth2ClientId?: pulumi.Input<string | undefined>;
    /**
     * Your oauth2 client secret can be found in the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.
     */
    oauth2ClientSecret?: pulumi.Input<string | undefined>;
    /**
     * API Password (this is a new password which you'll set or change in the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.)
     */
    oauth2Pass?: pulumi.Input<string | undefined>;
    /**
     * The oauth2 token url is https://auth.contabo.com/auth/realms/contabo/protocol/openid-connect/token.
     */
    oauth2TokenUrl?: pulumi.Input<string | undefined>;
    /**
     * API User (your email address to login to the [Customer Control Panel](https://new.contabo.com/account/security) under the menu item account secret.
     */
    oauth2User?: pulumi.Input<string | undefined>;
}

export namespace Provider {
    /**
     * The results of the Provider.terraformConfig method.
     */
    export interface TerraformConfigResult {
        readonly result: {[key: string]: any};
    }

}
