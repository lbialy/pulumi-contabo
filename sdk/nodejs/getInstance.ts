// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * The Compute Management API allows you to manage compute resources (e.g. creation, deletion, starting, stopping) as well as managing snapshots and custom images. It also supports [cloud-init](https://cloud-init.io/) at least on our default images (for custom images you will need to provide cloud-init support packages). The API offers providing cloud-init scripts via the userData field. Custom images must be provided in .qcow2 or .iso format.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as contabo from "@lbialy/contabo";
 *
 * // Search for a specific instance by ID
 * const testInstance = contabo.getInstance({
 *     id: "123455",
 * });
 * export const myTestInstance = testInstance;
 * ```
 */
export function getInstance(args: GetInstanceArgs, opts?: pulumi.InvokeOptions): Promise<GetInstanceResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("contabo:index/getInstance:getInstance", {
        "addOns": args.addOns,
        "cancelDate": args.cancelDate,
        "defaultUser": args.defaultUser,
        "displayName": args.displayName,
        "id": args.id,
        "imageId": args.imageId,
        "license": args.license,
        "productId": args.productId,
        "region": args.region,
        "sshKeys": args.sshKeys,
        "userData": args.userData,
    }, opts);
}

/**
 * A collection of arguments for invoking getInstance.
 */
export interface GetInstanceArgs {
    addOns?: inputs.GetInstanceAddOn[];
    /**
     * The date on which the instance will be cancelled.
     */
    cancelDate?: string;
    /**
     * Default user name created for login during (re-)installation with administrative privileges. Allowed values for Linux/BSD are admin (use sudo to apply administrative privileges like root) or root. Allowed values for Windows are admin (has administrative privileges like administrator) or administrator.See our [api documentation](https://api.contabo.com/#tag/Instances/operation/createInstance) for available default users.
     */
    defaultUser?: string;
    /**
     * Image Id is used to set up the compute instance. Ubuntu 20.04 is the default.
     */
    displayName?: string;
    /**
     * The identifier of the compute instance. Use it to manage it!
     */
    id: string;
    /**
     * Image Id is used to set up the compute instance. Ubuntu 20.04 is the default.
     */
    imageId?: string;
    /**
     * Additional license in order to enhance your chosen product. It is mainly needed for software licenses on your product (not needed for windows). See our [api documentation](https://api.contabo.com/#tag/Instances/operation/createInstance) for all available licenses.
     */
    license?: string;
    /**
     * Choose the VPS/VDS product you want to buy. See our products [here](https://api.contabo.com/#tag/Instances/operation/createInstance).
     */
    productId?: string;
    /**
     * Instance Region where the compute instance should be located. Default region is the EU. Following regions are available: `EU`,`US-central`,`US-east`,`US-west`,`SIN`.
     */
    region?: string;
    /**
     * Array of `secretIds` of public SSH keys for logging into as defaultUser with administrator/root privileges. Applies to Linux/BSD systems. Please refer to Secrets Management API.
     */
    sshKeys?: number[];
    /**
     * Cloud-Init Config in order to customize during start of compute instance.
     */
    userData?: string;
}

/**
 * A collection of values returned by getInstance.
 */
export interface GetInstanceResult {
    readonly addOns: outputs.GetInstanceAddOn[];
    /**
     * All other additional IP addresses of the instance.
     */
    readonly additionalIpsV4s: outputs.GetInstanceAdditionalIpsV4[];
    /**
     * The date on which the instance will be cancelled.
     */
    readonly cancelDate: string;
    /**
     * CPU core count of the instance.
     */
    readonly cpuCores: number;
    /**
     * The creation date of the compute instance.
     */
    readonly createdDate: string;
    /**
     * Default user name created for login during (re-)installation with administrative privileges. Allowed values for Linux/BSD are admin (use sudo to apply administrative privileges like root) or root. Allowed values for Windows are admin (has administrative privileges like administrator) or administrator.See our [api documentation](https://api.contabo.com/#tag/Instances/operation/createInstance) for available default users.
     */
    readonly defaultUser: string;
    /**
     * Image disk size of the instance in megabyte.
     */
    readonly diskMb: number;
    /**
     * Image Id is used to set up the compute instance. Ubuntu 20.04 is the default.
     */
    readonly displayName: string;
    /**
     * If the instance is in an error state (see status property), the error message can be seen in this field.
     */
    readonly errorMessage: string;
    /**
     * The identifier of the compute instance. Use it to manage it!
     */
    readonly id: string;
    /**
     * Image Id is used to set up the compute instance. Ubuntu 20.04 is the default.
     */
    readonly imageId: string;
    readonly ipConfigs: outputs.GetInstanceIpConfig[];
    /**
     * Time of the last update of the compute instance.
     */
    readonly lastUpdated: string;
    /**
     * Additional license in order to enhance your chosen product. It is mainly needed for software licenses on your product (not needed for windows). See our [api documentation](https://api.contabo.com/#tag/Instances/operation/createInstance) for all available licenses.
     */
    readonly license: string;
    /**
     * Mac address of the instance.
     */
    readonly macAddress: string;
    /**
     * Name of the compute instance.
     */
    readonly name: string;
    /**
     * Type of operating system (OS) installed on the instance.
     */
    readonly osType: string;
    /**
     * Initial contract period in months. Available periods are: 1, 3, 6 and 12 months. The default setting is 1 month.
     */
    readonly period: number;
    /**
     * Choose the VPS/VDS product you want to buy. See our products [here](https://api.contabo.com/#tag/Instances/operation/createInstance).
     */
    readonly productId: string;
    /**
     * InsInstance's category depending on Product Id. Following product types are available: `hdd`,`ssd`,`vds`,`nvme`.
     */
    readonly productType: string;
    /**
     * Image ram size in megabyte.
     */
    readonly ramMb: number;
    /**
     * Instance Region where the compute instance should be located. Default region is the EU. Following regions are available: `EU`,`US-central`,`US-east`,`US-west`,`SIN`.
     */
    readonly region: string;
    /**
     * Array of `secretIds` of public SSH keys for logging into as defaultUser with administrator/root privileges. Applies to Linux/BSD systems. Please refer to Secrets Management API.
     */
    readonly sshKeys: number[];
    /**
     * Status of the compute instance. The status can be set to `provisioning`, `uninstalled`, `running`, `stopped`, `error`, `installing`, `unknown`, or `installed`.
     */
    readonly status: string;
    /**
     * Cloud-Init Config in order to customize during start of compute instance.
     */
    readonly userData: string;
    /**
     * Identifier of the host system.
     */
    readonly vHostId: number;
}
/**
 * The Compute Management API allows you to manage compute resources (e.g. creation, deletion, starting, stopping) as well as managing snapshots and custom images. It also supports [cloud-init](https://cloud-init.io/) at least on our default images (for custom images you will need to provide cloud-init support packages). The API offers providing cloud-init scripts via the userData field. Custom images must be provided in .qcow2 or .iso format.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as contabo from "@lbialy/contabo";
 *
 * // Search for a specific instance by ID
 * const testInstance = contabo.getInstance({
 *     id: "123455",
 * });
 * export const myTestInstance = testInstance;
 * ```
 */
export function getInstanceOutput(args: GetInstanceOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetInstanceResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("contabo:index/getInstance:getInstance", {
        "addOns": args.addOns,
        "cancelDate": args.cancelDate,
        "defaultUser": args.defaultUser,
        "displayName": args.displayName,
        "id": args.id,
        "imageId": args.imageId,
        "license": args.license,
        "productId": args.productId,
        "region": args.region,
        "sshKeys": args.sshKeys,
        "userData": args.userData,
    }, opts);
}

/**
 * A collection of arguments for invoking getInstance.
 */
export interface GetInstanceOutputArgs {
    addOns?: pulumi.Input<pulumi.Input<inputs.GetInstanceAddOnArgs>[] | undefined>;
    /**
     * The date on which the instance will be cancelled.
     */
    cancelDate?: pulumi.Input<string | undefined>;
    /**
     * Default user name created for login during (re-)installation with administrative privileges. Allowed values for Linux/BSD are admin (use sudo to apply administrative privileges like root) or root. Allowed values for Windows are admin (has administrative privileges like administrator) or administrator.See our [api documentation](https://api.contabo.com/#tag/Instances/operation/createInstance) for available default users.
     */
    defaultUser?: pulumi.Input<string | undefined>;
    /**
     * Image Id is used to set up the compute instance. Ubuntu 20.04 is the default.
     */
    displayName?: pulumi.Input<string | undefined>;
    /**
     * The identifier of the compute instance. Use it to manage it!
     */
    id: pulumi.Input<string>;
    /**
     * Image Id is used to set up the compute instance. Ubuntu 20.04 is the default.
     */
    imageId?: pulumi.Input<string | undefined>;
    /**
     * Additional license in order to enhance your chosen product. It is mainly needed for software licenses on your product (not needed for windows). See our [api documentation](https://api.contabo.com/#tag/Instances/operation/createInstance) for all available licenses.
     */
    license?: pulumi.Input<string | undefined>;
    /**
     * Choose the VPS/VDS product you want to buy. See our products [here](https://api.contabo.com/#tag/Instances/operation/createInstance).
     */
    productId?: pulumi.Input<string | undefined>;
    /**
     * Instance Region where the compute instance should be located. Default region is the EU. Following regions are available: `EU`,`US-central`,`US-east`,`US-west`,`SIN`.
     */
    region?: pulumi.Input<string | undefined>;
    /**
     * Array of `secretIds` of public SSH keys for logging into as defaultUser with administrator/root privileges. Applies to Linux/BSD systems. Please refer to Secrets Management API.
     */
    sshKeys?: pulumi.Input<pulumi.Input<number>[] | undefined>;
    /**
     * Cloud-Init Config in order to customize during start of compute instance.
     */
    userData?: pulumi.Input<string | undefined>;
}
